import React from "react";

export const useGetAvatar = () => {
  const getAvatar = async () => {
    try {
    } catch {}
  };

  return { isLoading, error, getAvatar };
};

export default useAvatar;
